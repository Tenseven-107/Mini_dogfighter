[gd_scene load_steps=6 format=2]

[ext_resource path="res://Sprites/FX/Enemy_projectile.png" type="Texture" id=1]
[ext_resource path="res://Prefabs/Weapons/Bullet.tscn" type="PackedScene" id=2]

[sub_resource type="CircleShape2D" id=16]
radius = 8.0

[sub_resource type="Shader" id=15]
code = "shader_type canvas_item;

uniform vec4 line_color : hint_color = vec4(1);
uniform float line_thickness : hint_range(0, 10) = 1.0;

void fragment() {
	vec2 size = TEXTURE_PIXEL_SIZE * line_thickness;
	
	float outline = texture(TEXTURE, UV + vec2(-size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
	outline = min(outline, 1.0);
	
	vec4 color = texture(TEXTURE, UV);
	COLOR = mix(color, line_color, outline - color.a);
}"

[sub_resource type="ShaderMaterial" id=17]
shader = SubResource( 15 )
shader_param/line_color = Color( 1, 0.117647, 0, 0.262745 )
shader_param/line_thickness = 1.0

[node name="Bullet" instance=ExtResource( 2 )]
speed = 200

[node name="CollisionShape2D" parent="." index="0"]
shape = SubResource( 16 )

[node name="Sprite" parent="." index="1"]
material = SubResource( 17 )
texture = ExtResource( 1 )

[node name="Life_time" parent="." index="2"]
wait_time = 1.0
